server:
    port: 8081
    
eureka:
    client:
        fetch-registry: true
        register-with-eureka: true
        service-url:
            defaultZone: http://localhost:8761/eureka
    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true
    datasource:
        url: jdbc:mysql://localhost:3308/fornecedor_floricultura
        username: root
        password: 1234
        driver-class-name: org.mariadb.jdbc.Driver